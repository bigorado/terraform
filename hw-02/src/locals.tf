locals {
  vm_web = "${ var.vm_web_inst }"
  vm_db= "${ var.vm_db_inst }"
}

#locals {
#  vm_web = "${ var.vm_web_platf }-${ var.vm_web_inst }"
#  vm_db= "${ var.vm_db_platf }-${ var.vm_db_inst }"
#}
